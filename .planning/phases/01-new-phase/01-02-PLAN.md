---
phase: 01-new-phase
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/example/bootstrap/menu/domain/model/Menu.java
  - api/src/main/java/com/example/bootstrap/menu/domain/model/Role.java
  - api/src/main/java/com/example/bootstrap/menu/domain/model/UserRole.java
  - api/src/main/java/com/example/bootstrap/menu/domain/model/RoleMenuPermission.java
  - api/src/main/java/com/example/bootstrap/menu/domain/model/UserMenuPermission.java
  - api/src/main/java/com/example/bootstrap/menu/domain/repository/MenuRepository.java
  - api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleRepository.java
  - api/src/main/java/com/example/bootstrap/menu/domain/repository/UserRoleRepository.java
  - api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleMenuPermissionRepository.java
  - api/src/main/java/com/example/bootstrap/menu/domain/repository/UserMenuPermissionRepository.java
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - DOM-04
  - DOM-05

must_haves:
  truths:
    - "각 테이블에 대응하는 R2DBC 엔티티 5개(Menu, Role, UserRole, RoleMenuPermission, UserMenuPermission)가 컴파일된다"
    - "각 리포지토리를 통해 Reactive 방식으로(Mono/Flux) 기본 조회가 가능하다"
    - "별도 R2dbcConfig 수정 없이 menu 패키지가 자동 스캔된다"
  artifacts:
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/model/Menu.java"
      provides: "menus 테이블 R2DBC 엔티티"
      exports: ["Menu"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/model/Role.java"
      provides: "roles 테이블 R2DBC 엔티티"
      exports: ["Role"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/model/UserRole.java"
      provides: "user_roles 테이블 R2DBC 엔티티"
      exports: ["UserRole"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/model/RoleMenuPermission.java"
      provides: "role_menu_permissions 테이블 R2DBC 엔티티"
      exports: ["RoleMenuPermission"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/model/UserMenuPermission.java"
      provides: "user_menu_permissions 테이블 R2DBC 엔티티"
      exports: ["UserMenuPermission"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/repository/MenuRepository.java"
      provides: "Menu 리포지토리 — findAll(), findByCode()"
      exports: ["MenuRepository"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleRepository.java"
      provides: "Role 리포지토리 — findAll()"
      exports: ["RoleRepository"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/repository/UserRoleRepository.java"
      provides: "UserRole 리포지토리 — findByUserId(), findByRoleId()"
      exports: ["UserRoleRepository"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleMenuPermissionRepository.java"
      provides: "RoleMenuPermission 리포지토리 — findByRoleId(), findByRoleIdIn()"
      exports: ["RoleMenuPermissionRepository"]
    - path: "api/src/main/java/com/example/bootstrap/menu/domain/repository/UserMenuPermissionRepository.java"
      provides: "UserMenuPermission 리포지토리 — findByUserId()"
      exports: ["UserMenuPermissionRepository"]
  key_links:
    - from: "MenuRepository"
      to: "Menu"
      via: "ReactiveCrudRepository<Menu, Long>"
      pattern: "extends ReactiveCrudRepository"
    - from: "RoleMenuPermissionRepository"
      to: "RoleMenuPermission"
      via: "findByRoleIdIn(Collection<Long>)"
      pattern: "findByRoleIdIn"
---

<objective>
메뉴 RBAC 도메인 레이어를 구현한다. 5개 R2DBC 엔티티와 5개 리포지토리 인터페이스를 Account.java/AccountRepository.java 패턴 그대로 com.example.bootstrap.menu.domain 패키지에 생성한다.

Purpose: Phase 2의 MenuPermissionService가 이 리포지토리들을 Reactive 방식으로 조회할 수 있도록 도메인 계약을 확립한다.
Output: 10개 Java 파일 (엔티티 5개 + 리포지토리 5개)
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-new-phase/01-CONTEXT.md
@.planning/phases/01-new-phase/01-RESEARCH.md
@.planning/phases/01-new-phase/01-PATTERNS.md

<interfaces>
<!-- 아날로그 파일에서 추출한 패턴. 이 패턴을 그대로 복사한다. -->

Account.java (기준 엔티티 패턴):
  패키지: com.example.bootstrap.account.domain.model
  임포트:
    org.springframework.data.annotation.CreatedDate
    org.springframework.data.annotation.Id
    org.springframework.data.relational.core.mapping.Table
    java.time.LocalDateTime  ← V3 엔티티는 OffsetDateTime으로 교체 (D-05)
  클래스 구조:
    @Table("{table_name}")
    public class EntityName {
        @Id
        private Long id;
        // ... 필드
        @CreatedDate
        private OffsetDateTime createdAt;  // LocalDateTime이 아닌 OffsetDateTime (D-05)
        public EntityName() {}             // 기본 생성자 필수
        // 모든 필드에 Javadoc 포함한 getter/setter 수동 작성
        // getter: public ReturnType getFieldName() { return fieldName; }
        // setter: public void setFieldName(final ReturnType fieldName) { this.fieldName = fieldName; }
    }

AccountRepository.java (기준 리포지토리 패턴):
  패키지: com.example.bootstrap.account.domain.repository
  임포트:
    org.springframework.data.repository.reactive.ReactiveCrudRepository
    org.springframework.stereotype.Repository
    reactor.core.publisher.Mono  (단건 조회)
    reactor.core.publisher.Flux  (다건 조회)
  클래스 구조:
    /**
     * {설명} R2DBC 리포지토리.
     *
     * <p>{부가 설명}
     */
    @Repository
    public interface XxxRepository extends ReactiveCrudRepository<Xxx, Long> {
        // Spring Data 파생 쿼리 메서드 선언
        // findAll()은 ReactiveCrudRepository 기본 제공 — 별도 선언 불필요
    }

R2dbcConfig.java 확인사항:
  @EnableR2dbcRepositories(basePackages = "com.example.bootstrap")  ← menu 패키지 자동 스캔됨
  @EnableR2dbcAuditing  ← @CreatedDate 자동 채움 동작
  → R2dbcConfig.java 수정 불필요
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 R2DBC 엔티티 작성 (Menu, Role, UserRole, RoleMenuPermission, UserMenuPermission)</name>
  <files>
    api/src/main/java/com/example/bootstrap/menu/domain/model/Menu.java,
    api/src/main/java/com/example/bootstrap/menu/domain/model/Role.java,
    api/src/main/java/com/example/bootstrap/menu/domain/model/UserRole.java,
    api/src/main/java/com/example/bootstrap/menu/domain/model/RoleMenuPermission.java,
    api/src/main/java/com/example/bootstrap/menu/domain/model/UserMenuPermission.java
  </files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/account/domain/model/Account.java — 전체 파일 필독. 임포트 목록, @Table, @Id, @CreatedDate, 기본 생성자, Javadoc getter/setter 패턴을 이 파일에서 1:1 복사한다.
    - api/src/main/java/com/example/bootstrap/global/config/R2dbcConfig.java — @EnableR2dbcRepositories 스캔 범위 확인 (수정 없이 menu 패키지가 자동 스캔되는지 확인).
    - .planning/phases/01-new-phase/01-PATTERNS.md — 5개 엔티티 필드 구성 상세 확인 (패턴 맵의 각 엔티티 섹션).
  </read_first>
  <action>
    다음 규칙을 Account.java 패턴에서 엄격하게 따른다 (D-02):
    - @Column 어노테이션 사용 금지 — DefaultNamingStrategy가 자동 처리 (D-01).
    - Lombok @Data/@Getter/@Setter 사용 금지 — 수동 getter/setter 필수.
    - LocalDateTime 사용 금지 — 모든 타임스탬프 필드는 OffsetDateTime (D-05).
    - @LastModifiedDate 사용 금지 — V3 테이블에 updated_at 컬럼 없음.
    - @PrimaryKeyClass 복합 PK 사용 금지 — 서로게이트 BIGSERIAL PK 사용 (D-03).
    - Boolean 박싱 타입 사용 (primitive boolean 금지) — isActive, canRead, canWrite 전부 (RESEARCH.md Pitfall 2).
    - 패키지: com.example.bootstrap.menu.domain.model.

    Menu.java (@Table("menus")):
      필드: Long id (@Id), String code, String name, Integer displayOrder, Boolean isActive, OffsetDateTime createdAt (@CreatedDate).
      Boolean isActive getter: public Boolean isActive() { return isActive; } — isIsActive() 방지.

    Role.java (@Table("roles")):
      필드: Long id (@Id), String name, String description (nullable, 별도 어노테이션 없이 String만으로 충분), OffsetDateTime createdAt (@CreatedDate).

    UserRole.java (@Table("user_roles")):
      필드: Long id (@Id, 서로게이트 PK — D-03), Long userId, Long roleId, OffsetDateTime createdAt (@CreatedDate).
      주석: userId는 users.id FK, roleId는 roles.id FK임을 Javadoc에 명시.

    RoleMenuPermission.java (@Table("role_menu_permissions")):
      필드: Long id (@Id, 서로게이트 PK — D-03), Long roleId, Long menuId, Boolean canRead, Boolean canWrite, OffsetDateTime createdAt (@CreatedDate).
      Boolean getter 패턴: public Boolean isCanRead() { return canRead; }, public Boolean isCanWrite() { return canWrite; }.

    UserMenuPermission.java (@Table("user_menu_permissions")):
      필드: Long id (@Id, 서로게이트 PK — D-03), Long userId, Long menuId, Boolean canRead, Boolean canWrite, OffsetDateTime createdAt (@CreatedDate).
      Boolean getter 패턴: RoleMenuPermission.java와 동일, roleId 대신 userId.

    모든 setter 파라미터에 final 키워드 적용 (Account.java 패턴 동일).
    모든 getter/setter에 Javadoc 포함 (Account.java 50~61행 패턴 동일).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works && ./gradlew :api:compileJava --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 5개 파일이 com/example/bootstrap/menu/domain/model/ 경로에 존재한다.
    - 각 파일의 패키지 선언이 com.example.bootstrap.menu.domain.model이다.
    - grep -l "@Table" 결과로 5개 파일이 모두 열거된다.
    - grep -rn "LocalDateTime" api/src/main/java/com/example/bootstrap/menu/ 출력이 없다 (OffsetDateTime만 사용).
    - grep -rn "boolean isActive\|boolean canRead\|boolean canWrite" api/src/main/java/com/example/bootstrap/menu/ 출력이 없다 (Boolean 박싱 타입만 사용).
    - grep -rn "@Column" api/src/main/java/com/example/bootstrap/menu/domain/model/ 출력이 없다.
    - grep -rn "import lombok" api/src/main/java/com/example/bootstrap/menu/ 출력이 없다.
    - ./gradlew :api:compileJava 가 BUILD SUCCESSFUL을 반환한다.
  </acceptance_criteria>
  <done>5개 엔티티 파일이 생성되고 ./gradlew :api:compileJava가 성공한다.</done>
</task>

<task type="auto">
  <name>Task 2: 5개 리포지토리 인터페이스 작성 (MenuRepository ~ UserMenuPermissionRepository)</name>
  <files>
    api/src/main/java/com/example/bootstrap/menu/domain/repository/MenuRepository.java,
    api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleRepository.java,
    api/src/main/java/com/example/bootstrap/menu/domain/repository/UserRoleRepository.java,
    api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleMenuPermissionRepository.java,
    api/src/main/java/com/example/bootstrap/menu/domain/repository/UserMenuPermissionRepository.java
  </files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/account/domain/repository/AccountRepository.java — 전체 파일 필독. @Repository, extends ReactiveCrudRepository<Account, Long>, Javadoc, 파생 쿼리 메서드 패턴을 1:1 복사한다.
    - .planning/phases/01-new-phase/01-PATTERNS.md — 각 리포지토리의 파생 쿼리 메서드 목록 확인 (패턴 맵의 각 리포지토리 섹션).
  </read_first>
  <action>
    다음 규칙을 AccountRepository.java 패턴에서 엄격하게 따른다:
    - 패키지: com.example.bootstrap.menu.domain.repository.
    - 임포트: ReactiveCrudRepository, @Repository, Mono (단건), Flux (다건), java.util.Collection (findByRoleIdIn 용).
    - findAll()은 선언 불필요 — ReactiveCrudRepository가 기본 제공.
    - 모든 파생 쿼리 메서드에 Javadoc 필수.

    MenuRepository.java (ReactiveCrudRepository<Menu, Long>):
      추가 메서드: Mono<Menu> findByCode(String code) — 코드로 단건 조회 (없으면 empty Mono).

    RoleRepository.java (ReactiveCrudRepository<Role, Long>):
      추가 메서드: 없음 — findAll()은 ReactiveCrudRepository 기본 제공으로 충분.

    UserRoleRepository.java (ReactiveCrudRepository<UserRole, Long>):
      추가 메서드: Flux<UserRole> findByUserId(Long userId), Flux<UserRole> findByRoleId(Long roleId).

    RoleMenuPermissionRepository.java (ReactiveCrudRepository<RoleMenuPermission, Long>):
      추가 메서드: Flux<RoleMenuPermission> findByRoleId(Long roleId), Flux<RoleMenuPermission> findByRoleIdIn(Collection<Long> roleIds) — Phase 2 복수 역할 일괄 조회용, 지금 선언해 둔다.
      import java.util.Collection 필요.

    UserMenuPermissionRepository.java (ReactiveCrudRepository<UserMenuPermission, Long>):
      추가 메서드: Flux<UserMenuPermission> findByUserId(Long userId).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works && ./gradlew :api:compileJava --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 5개 파일이 com/example/bootstrap/menu/domain/repository/ 경로에 존재한다.
    - 각 파일의 패키지 선언이 com.example.bootstrap.menu.domain.repository이다.
    - grep -rn "@Repository" api/src/main/java/com/example/bootstrap/menu/domain/repository/ 출력이 5줄이다.
    - grep -rn "extends ReactiveCrudRepository" api/src/main/java/com/example/bootstrap/menu/domain/repository/ 출력이 5줄이다.
    - grep "findByRoleIdIn" api/src/main/java/com/example/bootstrap/menu/domain/repository/RoleMenuPermissionRepository.java 에서 해당 메서드 선언이 확인된다.
    - grep "findByCode" api/src/main/java/com/example/bootstrap/menu/domain/repository/MenuRepository.java 에서 해당 메서드 선언이 확인된다.
    - ./gradlew :api:compileJava 가 BUILD SUCCESSFUL을 반환한다 (엔티티 + 리포지토리 모두 포함).
  </acceptance_criteria>
  <done>5개 리포지토리 파일이 생성되고 ./gradlew :api:compileJava가 BUILD SUCCESSFUL을 반환한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → R2DBC Driver → PostgreSQL | Spring Data 파생 쿼리가 파라미터 바인딩을 통해 DB에 전달된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Spring Data 파생 쿼리 (findByUserId 등) | accept | Spring Data R2DBC가 PreparedStatement 파라미터 바인딩을 자동 생성한다. SQL 인젝션 불가. |
| T-02-02 | Information Disclosure | RoleMenuPermission.canRead / canWrite 필드 | accept | 리포지토리 단계에서는 데이터를 외부에 노출하지 않는다. API 노출은 Phase 2~3에서 별도 통제. |
| T-02-03 | Elevation of Privilege | @EnableR2dbcRepositories 전체 패키지 스캔 | accept | 기존 설정 그대로 사용. menu 패키지 신규 추가이므로 공격 면 확장 없음. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 패키지 설치 없음. 해당 없음. |
</threat_model>

<verification>
## Phase 1 Plan 02 완료 검증

```bash
# 엔티티 파일 존재 확인 (5개)
ls /Users/gyuha/workspace/office-works/api/src/main/java/com/example/bootstrap/menu/domain/model/

# 리포지토리 파일 존재 확인 (5개)
ls /Users/gyuha/workspace/office-works/api/src/main/java/com/example/bootstrap/menu/domain/repository/

# LocalDateTime 미사용 확인 (출력 없어야 함)
grep -rn "LocalDateTime" /Users/gyuha/workspace/office-works/api/src/main/java/com/example/bootstrap/menu/

# Lombok 미사용 확인 (출력 없어야 함)
grep -rn "import lombok" /Users/gyuha/workspace/office-works/api/src/main/java/com/example/bootstrap/menu/

# @Column 미사용 확인 (출력 없어야 함)
grep -rn "@Column" /Users/gyuha/workspace/office-works/api/src/main/java/com/example/bootstrap/menu/domain/model/

# 컴파일 성공 확인
cd /Users/gyuha/workspace/office-works && ./gradlew :api:compileJava 2>&1 | grep -E "BUILD SUCCESS|BUILD FAILED|error:"
```
</verification>

<success_criteria>
- 5개 엔티티 파일이 com.example.bootstrap.menu.domain.model 패키지에 생성된다.
- 5개 리포지토리 파일이 com.example.bootstrap.menu.domain.repository 패키지에 생성된다.
- 모든 타임스탬프 필드가 OffsetDateTime 타입이다 (LocalDateTime 없음).
- Boolean 박싱 타입이 사용된다 (isActive, canRead, canWrite).
- @Column 어노테이션 없음, Lombok 없음.
- ./gradlew :api:compileJava 가 BUILD SUCCESSFUL을 반환한다.
- R2dbcConfig.java가 변경되지 않았다.
</success_criteria>

<output>
완료 후 .planning/phases/01-new-phase/01-02-SUMMARY.md 를 생성한다.
</output>
