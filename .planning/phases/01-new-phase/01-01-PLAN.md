---
phase: 01-new-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/resources/db/migration/V3__menu_rbac.sql
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04
  - SCH-05
  - SCH-06

must_haves:
  truths:
    - "Flyway가 V3 마이그레이션을 실행하면 menus, roles, user_roles, role_menu_permissions, user_menu_permissions 5개 테이블이 생성된다"
    - "menus 테이블에 7개 초기 메뉴 데이터(DASHBOARD, USERS, TASKS, APPS, CHATS, SETTINGS, HELP_CENTER)가 INSERT된다"
    - "기존 V1/V2 마이그레이션 파일은 변경되지 않는다"
  artifacts:
    - path: "api/src/main/resources/db/migration/V3__menu_rbac.sql"
      provides: "5개 테이블 DDL + 초기 메뉴 INSERT"
      contains: "CREATE TABLE menus, CREATE TABLE roles, CREATE TABLE user_roles, CREATE TABLE role_menu_permissions, CREATE TABLE user_menu_permissions, INSERT INTO menus"
  key_links:
    - from: "user_roles.user_id"
      to: "users.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "fk_user_roles_user"
    - from: "role_menu_permissions.role_id"
      to: "roles.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "fk_role_menu_permissions_role"
    - from: "user_menu_permissions.user_id"
      to: "users.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "fk_user_menu_permissions_user"
---

<objective>
Flyway V3 마이그레이션 파일을 작성하여 메뉴 RBAC에 필요한 5개 테이블(menus, roles, user_roles, role_menu_permissions, user_menu_permissions)을 생성하고, 초기 메뉴 7개를 INSERT한다.

Purpose: Phase 2의 권한 병합 서비스가 쿼리할 DB 구조를 확립한다.
Output: api/src/main/resources/db/migration/V3__menu_rbac.sql (신규 파일)
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-new-phase/01-CONTEXT.md
@.planning/phases/01-new-phase/01-RESEARCH.md
@.planning/phases/01-new-phase/01-PATTERNS.md

<interfaces>
<!-- V1__init.sql에서 추출한 SQL 스타일 규칙. 이 패턴을 그대로 따른다. -->

파일 헤더 패턴:
  -- =============================================================================
  -- V{n}__{설명}.sql — {설명}
  -- =============================================================================

테이블 섹션 구분자:
  -- ─────────────────────────────────────────────────────────────────────────────
  -- {n}. {table_name}
  -- ─────────────────────────────────────────────────────────────────────────────

컬럼 스타일: 정렬된 공백 (컬럼명 ~16자 고정폭), BIGSERIAL PRIMARY KEY, NOT NULL DEFAULT 명시

제약명 규칙:
  - UNIQUE: uq_{table}_{column}  (예: uq_menus_code)
  - FK:     fk_{table}_{ref_table}  (예: fk_user_roles_user)
  - CHECK:  chk_{table}_{field}

인덱스 패턴:
  CREATE INDEX idx_{table}_{column} ON {table} ({column});

COMMENT ON 패턴:
  COMMENT ON TABLE  {table}         IS '한국어 설명';
  COMMENT ON COLUMN {table}.{col}   IS '한국어 설명';

타임스탬프: V3부터 TIMESTAMPTZ 사용 (V1/V2의 TIMESTAMP와 다름 — D-05 결정)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V3__menu_rbac.sql 작성 — 5개 테이블 DDL + 초기 데이터 INSERT</name>
  <files>api/src/main/resources/db/migration/V3__menu_rbac.sql</files>
  <read_first>
    - api/src/main/resources/db/migration/V1__init.sql — 파일 헤더, 섹션 구분자, 컬럼 정렬 스타일, CONSTRAINT 명명, COMMENT ON, CREATE INDEX 패턴 전체 확인 (이 파일의 스타일을 그대로 복사한다)
    - api/src/main/resources/db/migration/V2__batch_schema.sql — V3 파일명 확인용 (V1, V2 다음 = V3__. 이 파일들은 절대 수정하지 않는다)
    - .planning/phases/01-new-phase/01-CONTEXT.md — D-03(서로게이트 PK), D-05(TIMESTAMPTZ), D-08(메뉴 7개 목록), D-09(UPPER_SNAKE_CASE) 잠긴 결정 확인
  </read_first>
  <action>
    신규 파일 api/src/main/resources/db/migration/V3__menu_rbac.sql을 생성한다. V1__init.sql의 파일 헤더 스타일과 섹션 구분자를 그대로 따른다.

    파일 헤더: "V3__menu_rbac.sql — 메뉴 RBAC 스키마", 포함 테이블 5개 목록 주석.

    테이블 1 — menus (SCH-01): id BIGSERIAL PRIMARY KEY, code VARCHAR(50) NOT NULL, name VARCHAR(100) NOT NULL, display_order INTEGER NOT NULL DEFAULT 0, is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. 제약: CONSTRAINT uq_menus_code UNIQUE (code). 인덱스: idx_menus_display_order ON menus (display_order). COMMENT ON TABLE/COLUMN 한국어.

    테이블 2 — roles (SCH-02): id BIGSERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, description TEXT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. 제약: CONSTRAINT uq_roles_name UNIQUE (name). COMMENT ON TABLE/COLUMN 한국어.

    테이블 3 — user_roles (SCH-03, D-03 서로게이트 PK): id BIGSERIAL PRIMARY KEY, user_id BIGINT NOT NULL, role_id BIGINT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. 제약: CONSTRAINT uq_user_roles_user_role UNIQUE (user_id, role_id), CONSTRAINT fk_user_roles_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE, CONSTRAINT fk_user_roles_role FOREIGN KEY (role_id) REFERENCES roles (id) ON DELETE CASCADE. 인덱스: idx_user_roles_user_id ON user_roles (user_id), idx_user_roles_role_id ON user_roles (role_id). COMMENT ON 한국어.

    테이블 4 — role_menu_permissions (SCH-04, D-03 서로게이트 PK): id BIGSERIAL PRIMARY KEY, role_id BIGINT NOT NULL, menu_id BIGINT NOT NULL, can_read BOOLEAN NOT NULL DEFAULT FALSE, can_write BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. 제약: CONSTRAINT uq_role_menu_permissions_role_menu UNIQUE (role_id, menu_id), CONSTRAINT fk_role_menu_permissions_role FOREIGN KEY (role_id) REFERENCES roles (id) ON DELETE CASCADE, CONSTRAINT fk_role_menu_permissions_menu FOREIGN KEY (menu_id) REFERENCES menus (id) ON DELETE CASCADE. 인덱스: idx_role_menu_permissions_role_id ON role_menu_permissions (role_id). COMMENT ON 한국어.

    테이블 5 — user_menu_permissions (SCH-05, D-03 서로게이트 PK): id BIGSERIAL PRIMARY KEY, user_id BIGINT NOT NULL, menu_id BIGINT NOT NULL, can_read BOOLEAN NOT NULL DEFAULT FALSE, can_write BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. 제약: CONSTRAINT uq_user_menu_permissions_user_menu UNIQUE (user_id, menu_id), CONSTRAINT fk_user_menu_permissions_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE, CONSTRAINT fk_user_menu_permissions_menu FOREIGN KEY (menu_id) REFERENCES menus (id) ON DELETE CASCADE. 인덱스: idx_user_menu_permissions_user_id ON user_menu_permissions (user_id). COMMENT ON 한국어.

    초기 데이터 (SCH-06, D-08): INSERT INTO menus (display_order, code, name, is_active) VALUES 구문으로 7개 행을 한 번에 INSERT.
    행 목록 (D-09: code는 UPPER_SNAKE_CASE):
      (1, 'DASHBOARD',   '대시보드',    TRUE),
      (2, 'USERS',       '사용자',      TRUE),
      (3, 'TASKS',       '작업',        TRUE),
      (4, 'APPS',        '앱',          TRUE),
      (5, 'CHATS',       '채팅',        TRUE),
      (6, 'SETTINGS',    '설정',        TRUE),
      (7, 'HELP_CENTER', '도움말 센터', TRUE)

    주의사항:
    - V1/V2 파일은 절대 수정하지 않는다 (D-07).
    - 모든 created_at 컬럼은 TIMESTAMPTZ 사용 (TIMESTAMP 사용 금지 — D-05).
    - SERIAL 대신 BIGSERIAL 사용 (V1 패턴 동일).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" /Users/gyuha/workspace/office-works/api/src/main/resources/db/migration/V3__menu_rbac.sql</automated>
  </verify>
  <acceptance_criteria>
    - V3__menu_rbac.sql 파일이 존재한다.
    - grep -c "CREATE TABLE" V3__menu_rbac.sql 출력값이 5이다 (menus, roles, user_roles, role_menu_permissions, user_menu_permissions).
    - grep -c "TIMESTAMPTZ" V3__menu_rbac.sql 출력값이 5 이상이다 (각 테이블 created_at마다 1개 이상).
    - grep -c "BIGSERIAL" V3__menu_rbac.sql 출력값이 5이다 (각 테이블 PK).
    - grep "INSERT INTO menus" V3__menu_rbac.sql가 존재하고, 'HELP_CENTER' 문자열이 파일에 포함된다.
    - grep "uq_menus_code\|uq_roles_name\|uq_user_roles_user_role\|uq_role_menu_permissions_role_menu\|uq_user_menu_permissions_user_menu" V3__menu_rbac.sql의 출력 행이 5줄이다.
    - V1__init.sql과 V2__batch_schema.sql의 파일 내용이 변경되지 않았다 (git diff --name-only에서 V1, V2 파일이 나타나지 않는다).
  </acceptance_criteria>
  <done>Flyway V3 마이그레이션 파일이 생성되어 5개 테이블 DDL과 7개 초기 메뉴 INSERT를 포함한다. V1/V2 파일은 미변경이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway → PostgreSQL | Flyway가 JDBC를 통해 PostgreSQL에 DDL/DML을 실행한다. 이 경계에서 SQL 인젝션 가능성을 검토한다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | V3__menu_rbac.sql (정적 SQL) | accept | Flyway 마이그레이션은 정적 문자열 — 런타임 파라미터 없음. SQL 인젝션 불가. |
| T-01-02 | Tampering | FK ON DELETE CASCADE | accept | ON DELETE CASCADE는 의도된 참조 무결성 보장 수단. users 삭제 시 연관 권한 행도 삭제됨 — 데이터 누출 없음. |
| T-01-03 | Information Disclosure | menus, roles 테이블 컬럼 | accept | 코드/이름/순서 등 비민감 마스터 데이터. PII 없음, 저가치 타겟. |
| T-01-04 | Tampering | Flyway 체크섬 | mitigate | V3 파일을 DB 적용 전 최종 확정한다. 적용 후 수정 시 FlywayException 발생으로 앱 시작 실패. 코드 리뷰로 사전 방지. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 패키지 설치 없음. 해당 없음. |
</threat_model>

<verification>
## Phase 1 Plan 01 완료 검증

```bash
# 파일 존재 확인
ls -la /Users/gyuha/workspace/office-works/api/src/main/resources/db/migration/V3__menu_rbac.sql

# CREATE TABLE 5개 확인
grep -c "CREATE TABLE" /Users/gyuha/workspace/office-works/api/src/main/resources/db/migration/V3__menu_rbac.sql
# 기대값: 5

# TIMESTAMPTZ 사용 확인 (TIMESTAMP가 아닌)
grep -c "TIMESTAMPTZ" /Users/gyuha/workspace/office-works/api/src/main/resources/db/migration/V3__menu_rbac.sql
# 기대값: 5 이상

# 초기 데이터 INSERT 확인
grep "HELP_CENTER" /Users/gyuha/workspace/office-works/api/src/main/resources/db/migration/V3__menu_rbac.sql
# 기대값: 'HELP_CENTER' 포함 행 출력

# V1/V2 변경 없음 확인
git -C /Users/gyuha/workspace/office-works diff --name-only V1__init.sql V2__batch_schema.sql
# 기대값: 출력 없음
```
</verification>

<success_criteria>
- V3__menu_rbac.sql이 5개 테이블 DDL(menus, roles, user_roles, role_menu_permissions, user_menu_permissions)을 포함한다.
- 모든 created_at 컬럼이 TIMESTAMPTZ 타입이다.
- menus 테이블에 7개 행(DASHBOARD ~ HELP_CENTER) INSERT 구문이 존재한다.
- 3개 연결 테이블(user_roles, role_menu_permissions, user_menu_permissions)이 서로게이트 BIGSERIAL PK와 UNIQUE 복합 제약을 모두 가진다.
- V1__init.sql, V2__batch_schema.sql은 변경되지 않았다.
</success_criteria>

<output>
완료 후 .planning/phases/01-new-phase/01-01-SUMMARY.md 를 생성한다.
</output>
