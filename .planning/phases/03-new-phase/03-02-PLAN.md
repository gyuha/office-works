---
phase: 03-new-phase
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/example/bootstrap/global/security/ApiAccessDeniedHandler.java
  - api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java
autonomous: true
requirements:
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "403 응답이 ApiResponse envelope 형식({code:'MENU_002', message:'...'})으로 반환된다"
    - "/api/menus/** 경로에 인증이 명시적으로 요구된다 (SecurityConfig authorizeExchange)"
    - "401 응답은 기존 HttpStatusServerEntryPoint가 그대로 처리한다"
  artifacts:
    - path: "api/src/main/java/com/example/bootstrap/global/security/ApiAccessDeniedHandler.java"
      provides: "ServerAccessDeniedHandler 구현 — 403 ApiResponse envelope 반환"
      contains: "implements ServerAccessDeniedHandler"
    - path: "api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java"
      provides: "accessDeniedHandler 등록 + /api/menus/** authenticated 경로 추가"
      contains: "accessDeniedHandler"
  key_links:
    - from: "SecurityConfig.exceptionHandling()"
      to: "ApiAccessDeniedHandler.handle()"
      via: ".accessDeniedHandler(apiAccessDeniedHandler)"
      pattern: "accessDeniedHandler"
    - from: "ApiAccessDeniedHandler"
      to: "ApiResponse.error('MENU_002', '...')"
      via: "ObjectMapper.writeValueAsBytes()"
      pattern: "MENU_002"
---

<objective>
403 Forbidden 커스텀 응답 핸들러를 구현하고 SecurityConfig에 경로 등록 및 핸들러를 연결한다.

Purpose: 권한 없는 요청 시 Spring Security 기본 403 빈 응답 대신 ApiResponse envelope 형식으로 반환한다 (SEC-04 per D-05~D-09). SecurityConfig에 /api/menus/** 명시적 인증 요구를 추가한다 (SEC-03 per D-10).
Output: ApiAccessDeniedHandler 신규 클래스 + SecurityConfig 2곳 수정
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/office-works/.planning/PROJECT.md
@/Users/gyuha/workspace/office-works/.planning/ROADMAP.md

<interfaces>
<!-- 기존 코드 — Executor는 이 계약을 직접 사용한다. 탐색 불필요. -->

From global/config/SecurityConfig.java (수정 대상):
  @Bean
  public SecurityWebFilterChain securityWebFilterChain(
          final ServerHttpSecurity http,
          final JwtAuthenticationFilter jwtAuthenticationFilter) {
      return http
          .csrf(csrf -> csrf.disable())
          .httpBasic(httpBasic -> httpBasic.disable())
          .formLogin(login -> login.disable())
          .exceptionHandling(ex -> ex
              .authenticationEntryPoint(new HttpStatusServerEntryPoint(HttpStatus.UNAUTHORIZED)))
              // ← 여기에 .accessDeniedHandler(apiAccessDeniedHandler) 추가
          .addFilterAt(jwtAuthenticationFilter, SecurityWebFiltersOrder.AUTHENTICATION)
          .authorizeExchange(exchanges -> exchanges
              .pathMatchers("/actuator/health", "/actuator/health/**", "/actuator/info", "/actuator/prometheus").permitAll()
              .pathMatchers("/actuator/**").hasRole("ADMIN")
              .pathMatchers("/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html", "/webjars/**").permitAll()
              .pathMatchers(HttpMethod.POST, "/api/v1/auth/**").permitAll()
              .pathMatchers("/api/v1/admin/**").hasRole("ADMIN")
              // ← 여기에 .pathMatchers("/api/menus/**").authenticated() 추가 (SEC-03)
              .anyExchange().authenticated()
          )
          .build();
  }

From global/response/ApiResponse.java:
  public static <T> ApiResponse<T> error(final String code, final String message) {
      return new ApiResponse<>(code, message, null, null);
  }

From global/exception/ErrorCode.java:
  MENU_002(HttpStatus.FORBIDDEN, "MENU_002")  ← 이미 존재

Spring Security WebFlux — ServerAccessDeniedHandler:
  public interface ServerAccessDeniedHandler {
      Mono<Void> handle(ServerWebExchange exchange, AccessDeniedException denied);
  }

i18n 메시지: messages.properties, messages_ko.properties에 MENU_002 키 없음 → 하드코딩 허용 (D-08)

기존 패키지 위치 (D-09):
  global/security/jwt/JwtAuthenticationFilter.java  ← jwt 서브패키지
  global/security/MenuAuthorizationBean.java         ← global/security/ 직접
  → ApiAccessDeniedHandler도 global/security/ 직접 (jwt 서브패키지 아님)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApiAccessDeniedHandler 신규 구현</name>
  <files>api/src/main/java/com/example/bootstrap/global/security/ApiAccessDeniedHandler.java</files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java (동일 패키지 패턴)
    - api/src/main/java/com/example/bootstrap/global/response/ApiResponse.java (error 팩토리)
    - api/src/main/java/com/example/bootstrap/global/exception/ErrorCode.java (MENU_002 확인)
  </read_first>
  <action>
    패키지 `com.example.bootstrap.global.security`에 `ApiAccessDeniedHandler` 클래스를 신규 생성한다 (D-09 per CONTEXT — 클래스명은 도메인 무관한 공용 이름 선택).

    구현 명세:
    - `@Component` 어노테이션 적용
    - `ServerAccessDeniedHandler` 인터페이스 구현
    - 생성자 주입: `ObjectMapper objectMapper` (Lombok 금지, @Autowired 필드 주입 금지)
    - `handle(ServerWebExchange exchange, AccessDeniedException denied)` 메서드:
      1. `response.setStatusCode(HttpStatus.FORBIDDEN)`
      2. `response.getHeaders().setContentType(MediaType.APPLICATION_JSON)`
      3. body: `ApiResponse.error("MENU_002", "메뉴 접근 권한이 없습니다.")` (D-08 — MENU_002 키 i18n 없으므로 하드코딩)
      4. `objectMapper.writeValueAsBytes(body)` → try/catch: 직렬화 실패 시 `Mono.error(new RuntimeException(e))`
      5. `response.getHeaders().setContentLength(bytes.length)`
      6. `DataBuffer buffer = exchange.getResponse().bufferFactory().wrap(bytes)`
      7. `return exchange.getResponse().writeWith(Mono.just(buffer))`

    필요한 import:
    - `com.example.bootstrap.global.response.ApiResponse`
    - `com.fasterxml.jackson.databind.ObjectMapper`
    - `org.springframework.core.io.buffer.DataBuffer`
    - `org.springframework.http.HttpStatus`
    - `org.springframework.http.MediaType`
    - `org.springframework.security.access.AccessDeniedException`
    - `org.springframework.security.web.server.authorization.ServerAccessDeniedHandler`
    - `org.springframework.stereotype.Component`
    - `org.springframework.web.server.ServerWebExchange`
    - `reactor.core.publisher.Mono`

    JavaDoc: "메뉴 접근 권한 없는 요청 시 ApiResponse envelope 형식의 403 Forbidden 응답을 반환합니다."
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <done>ApiAccessDeniedHandler.java 존재, ServerAccessDeniedHandler 구현, MENU_002 코드 포함, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: SecurityConfig — accessDeniedHandler 등록 + /api/menus/** 경로 추가</name>
  <files>api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java</files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java (기존 전체 구조)
  </read_first>
  <action>
    기존 `SecurityConfig.java`를 수정한다. 두 곳을 변경한다:

    변경 1 — securityWebFilterChain 메서드 파라미터에 `ApiAccessDeniedHandler apiAccessDeniedHandler` 추가 (생성자/메서드 파라미터 주입):
    ```
    public SecurityWebFilterChain securityWebFilterChain(
            final ServerHttpSecurity http,
            final JwtAuthenticationFilter jwtAuthenticationFilter,
            final ApiAccessDeniedHandler apiAccessDeniedHandler)  ← 추가
    ```

    변경 2 — exceptionHandling 블록에 `.accessDeniedHandler(apiAccessDeniedHandler)` 체이닝 추가 (D-07):
    ```java
    .exceptionHandling(ex -> ex
        .authenticationEntryPoint(new HttpStatusServerEntryPoint(HttpStatus.UNAUTHORIZED))
        .accessDeniedHandler(apiAccessDeniedHandler))  ← 추가
    ```

    변경 3 — authorizeExchange 블록에 `.pathMatchers("/api/menus/**").authenticated()` 추가 (SEC-03, D-10). 위치는 `.pathMatchers("/api/v1/admin/**").hasRole("ADMIN")` 다음, `.anyExchange().authenticated()` 직전:
    ```java
    .pathMatchers("/api/v1/admin/**").hasRole("ADMIN")
    .pathMatchers("/api/menus/**").authenticated()  ← 추가
    .anyExchange().authenticated()
    ```

    변경 4 — 필요한 import 추가:
    - `com.example.bootstrap.global.security.ApiAccessDeniedHandler`

    주의:
    - JwtAuthenticationFilter는 변경하지 않는다 (D-13).
    - 기존 주석, JavaDoc, 코드 스타일을 그대로 유지한다.
    - `anyExchange().authenticated()` 앞에 `/api/menus/**`를 배치하여 명시적으로 먼저 매칭되게 한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <done>SecurityConfig.java에 accessDeniedHandler 체이닝 존재, /api/menus/** pathMatcher 존재, 컴파일 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spring Security → ApiAccessDeniedHandler | 인증은 됐으나 권한 없는 요청이 핸들러에 도달 |
| SecurityConfig authorizeExchange → /api/menus/** | 미인증 요청이 인증 체크를 통과하려는 시도 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | ApiAccessDeniedHandler 에러 응답 | accept | 에러 코드(MENU_002)와 메시지만 노출. 스택 트레이스/내부 정보 없음 |
| T-03-05 | Denial of Service | ObjectMapper.writeValueAsBytes() | accept | ApiResponse 크기 고정(<200B). 직렬화 실패 시 Mono.error()로 전파 |
| T-03-06 | Elevation of Privilege | /api/menus/** anyExchange() 우선순위 | mitigate | pathMatchers("/api/menus/**").authenticated()를 anyExchange() 앞에 배치하여 명시적 순서 보장 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
- `./gradlew compileJava` 성공
- ApiAccessDeniedHandler 파일 존재: `ls api/src/main/java/com/example/bootstrap/global/security/ApiAccessDeniedHandler.java`
- SecurityConfig에 accessDeniedHandler 확인: `grep -c "accessDeniedHandler" api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java`
- SecurityConfig에 /api/menus/** 확인: `grep -c "api/menus" api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java`
- ApiAccessDeniedHandler에 MENU_002 확인: `grep -c "MENU_002" api/src/main/java/com/example/bootstrap/global/security/ApiAccessDeniedHandler.java`
</verification>

<success_criteria>
1. ApiAccessDeniedHandler.java가 global/security/ 패키지에 존재하며 ServerAccessDeniedHandler를 구현한다
2. 403 응답 본문에 "MENU_002" 코드가 포함된 ApiResponse envelope이 반환된다
3. SecurityConfig.exceptionHandling()에 .accessDeniedHandler(apiAccessDeniedHandler) 체이닝이 추가된다
4. SecurityConfig.authorizeExchange()에 .pathMatchers("/api/menus/**").authenticated()가 anyExchange() 앞에 추가된다
5. JwtAuthenticationFilter는 변경되지 않는다
6. 전체 컴파일 성공
</success_criteria>

<output>
완료 후 `.planning/phases/03-new-phase/03-02-SUMMARY.md` 생성
</output>
