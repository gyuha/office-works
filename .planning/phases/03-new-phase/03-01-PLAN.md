---
phase: 03-new-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/example/bootstrap/menu/application/dto/AdminMenuResponse.java
  - api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java
autonomous: true
requirements:
  - API-02
  - SEC-02

must_haves:
  truths:
    - "ADMIN 토큰으로 GET /api/menus를 호출하면 전체 메뉴 목록(menuId, code, name, displayOrder, isActive)을 반환한다"
    - "USER 토큰 또는 미인증 요청은 SecurityConfig/PreAuthorize에 의해 차단된다 (403 또는 401)"
  artifacts:
    - path: "api/src/main/java/com/example/bootstrap/menu/application/dto/AdminMenuResponse.java"
      provides: "ADMIN 전용 메뉴 응답 DTO (Java record)"
      contains: "record AdminMenuResponse"
    - path: "api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java"
      provides: "GET /api/menus 엔드포인트 (@PreAuthorize + MenuRepository 호출)"
      contains: "@GetMapping"
  key_links:
    - from: "MenuController.getMenus()"
      to: "MenuRepository.findAllByOrderByDisplayOrderAsc()"
      via: "Flux → collectList()"
      pattern: "menuRepository\\.findAll"
    - from: "MenuController.getMenus()"
      to: "AdminMenuResponse"
      via: "Menu 엔티티 → DTO 매핑"
      pattern: "AdminMenuResponse"
---

<objective>
ADMIN 전용 전체 메뉴 조회 API를 구현한다.

Purpose: GET /api/menus 엔드포인트를 추가하여 ADMIN이 모든 메뉴를 조회할 수 있게 한다. @PreAuthorize("hasRole('ADMIN')")로 역할 체크를 Controller 레이어에서 처리한다 (SEC-02 per D-11).
Output: AdminMenuResponse DTO record + MenuController에 getMenus() 메서드 추가
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/office-works/.planning/PROJECT.md
@/Users/gyuha/workspace/office-works/.planning/ROADMAP.md

<interfaces>
<!-- 기존 코드 — Executor는 이 계약을 직접 사용한다. 탐색 불필요. -->

From menu/domain/model/Menu.java:
  - getId(): Long
  - getCode(): String
  - getName(): String
  - getDisplayOrder(): Integer
  - isActive(): Boolean  ← Boolean (박싱 타입), null-safe 필요 시 Boolean.TRUE.equals() 사용

From menu/domain/repository/MenuRepository.java (ReactiveCrudRepository 확장):
  - findByCode(String code): Mono<Menu>
  - findByIsActiveTrueOrderByDisplayOrderAsc(): Flux<Menu>
  - findAll(): Flux<Menu>  ← ReactiveCrudRepository 기본 제공 (정렬 없음)

From menu/application/dto/MyMenuResponse.java (참조용 — 이 파일은 수정하지 않음):
  public record MyMenuResponse(Long menuId, String code, String name, boolean canRead, boolean canWrite) {}

From global/response/ApiResponse.java:
  - ApiResponse.success(String message, T data): ApiResponse<T>
  - ApiResponse.error(String code, String message): ApiResponse<T>

From menu/controller/MenuController.java (기존 — 수정 대상):
  @RestController
  @RequestMapping("/api/menus")
  public class MenuController {
      private final MenuPermissionService menuPermissionService;
      public MenuController(final MenuPermissionService menuPermissionService) { ... }
      @GetMapping("/my")
      public Mono<ResponseEntity<ApiResponse<List<MyMenuResponse>>>> getMyMenus(Authentication auth) { ... }
  }

From batch/controller/BatchController.java (@PreAuthorize 패턴):
  // @PreAuthorize는 클래스 레벨 혹은 메서드 레벨에 적용
  // SecurityConfig에서 /api/v1/admin/** 경로 hasRole("ADMIN")으로 등록 + Controller에는 별도 @PreAuthorize 없음
  // Phase 3에서는 MenuController의 getMenus()에 @PreAuthorize("hasRole('ADMIN')")를 메서드 레벨로 추가
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminMenuResponse DTO record 생성</name>
  <files>api/src/main/java/com/example/bootstrap/menu/application/dto/AdminMenuResponse.java</files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/menu/application/dto/MyMenuResponse.java (DTO record 패턴)
    - api/src/main/java/com/example/bootstrap/menu/domain/model/Menu.java (필드 타입 확인)
  </read_first>
  <action>
    패키지 `com.example.bootstrap.menu.application.dto`에 Java record `AdminMenuResponse`를 신규 생성한다 (D-01, D-02, D-03 per CONTEXT).

    필드 5개 (D-02):
    - menuId: Long (Menu.getId() 대응)
    - code: String (Menu.getCode() 대응)
    - name: String (Menu.getName() 대응)
    - displayOrder: Integer (Menu.getDisplayOrder() 대응 — Menu 엔티티 타입이 Integer이므로 동일)
    - isActive: Boolean (Menu.isActive() 대응 — Menu 엔티티 타입이 Boolean 박싱이므로 동일)

    주의: `canRead`/`canWrite` 필드를 포함하지 않는다 (D-02). MyMenuResponse와 필드 구성이 다른 별개 DTO이다.

    JavaDoc: "ADMIN 전용 전체 메뉴 조회 응답 DTO." + 파라미터 5개 설명.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <done>AdminMenuResponse.java 파일 존재, record 선언에 5개 필드(menuId, code, name, displayOrder, isActive) 포함, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: MenuController에 GET /api/menus 메서드 추가</name>
  <files>api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java</files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java (기존 구조, 수정 대상)
    - api/src/main/java/com/example/bootstrap/menu/domain/repository/MenuRepository.java (사용 가능한 메서드)
  </read_first>
  <action>
    기존 `MenuController`를 수정하여 다음을 추가한다:

    1. 생성자에 `MenuRepository menuRepository` 파라미터 추가 (생성자 주입 — D-19 carry-forward, @Autowired 금지).
       기존 MenuPermissionService는 그대로 유지.

    2. getMenus() 메서드 추가:
       - 어노테이션: `@GetMapping` (경로 없음 → "/api/menus"에 매핑), `@PreAuthorize("hasRole('ADMIN')")` (SEC-02, D-11)
       - 반환 타입: `Mono<ResponseEntity<ApiResponse<List<AdminMenuResponse>>>>`
       - Authentication 파라미터 불필요 (ADMIN 체크는 @PreAuthorize가 담당)
       - 구현: `menuRepository.findAll()` → `.collectSortedList(Comparator.comparingInt(m -> m.getDisplayOrder() != null ? m.getDisplayOrder() : 0))` → AdminMenuResponse 매핑 → ResponseEntity.ok(ApiResponse.success("전체 메뉴 목록을 조회했습니다.", list))

       AdminMenuResponse 매핑 시:
       - new AdminMenuResponse(m.getId(), m.getCode(), m.getName(), m.getDisplayOrder(), m.isActive())
       - isActive는 Boolean 박싱 타입 — null 가능성 있음. Boolean.TRUE.equals(m.isActive()) 결과를 isActive 필드에 그대로 전달하지 말 것. AdminMenuResponse.isActive는 Boolean 타입이므로 m.isActive() 그대로 전달 (null-safe 체크는 직렬화 시 nullable로 전달됨 — record Boolean 타입 유지).

    3. 필요한 import 추가: `MenuRepository`, `AdminMenuResponse`, `Comparator`, `org.springframework.security.access.prepost.PreAuthorize`

    주의: `/my` 엔드포인트(getMyMenus)는 변경하지 않는다. 기존 코드 스타일(한국어 JavaDoc, 들여쓰기, 패턴)을 그대로 따른다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <done>MenuController.java에 getMenus() 메서드 존재, @PreAuthorize("hasRole('ADMIN')") 포함, MenuRepository 생성자 주입, 컴파일 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → GET /api/menus | 인증되지 않거나 ADMIN 아닌 사용자의 접근 시도 |
| @PreAuthorize SpEL → SecurityContext | JWT authorities 클레임 위변조 시도 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | GET /api/menus @PreAuthorize | mitigate | hasRole('ADMIN')로 ROLE_ADMIN 보유 여부만 체크 — JWT authorities 클레임 기반 (DB 재조회 없음, D-18) |
| T-03-02 | Information Disclosure | AdminMenuResponse | accept | 메뉴 코드/이름은 저위험 정보. PII 없음 |
| T-03-03 | Tampering | JWT authorities 위변조 | mitigate | JwtTokenProvider가 서명 검증 — 변조 시 AUTH_001 반환. JwtAuthenticationFilter 변경 없음 (D-13) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
- `./gradlew compileJava` 성공
- AdminMenuResponse.java 파일 존재: `api/src/main/java/com/example/bootstrap/menu/application/dto/AdminMenuResponse.java`
- MenuController.java에 `@PreAuthorize("hasRole('ADMIN')")` 포함 확인: `grep -c "PreAuthorize" api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java`
- MenuController.java에 MenuRepository 생성자 주입 확인: `grep -c "MenuRepository" api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java`
</verification>

<success_criteria>
1. AdminMenuResponse record가 menuId, code, name, displayOrder, isActive 5개 필드를 포함한다
2. MenuController.getMenus()에 @PreAuthorize("hasRole('ADMIN')") 적용
3. MenuController 생성자가 MenuRepository를 주입받는다
4. 전체 컴파일 성공 (`./gradlew compileJava`)
5. 기존 getMyMenus() 메서드는 변경되지 않는다
</success_criteria>

<output>
완료 후 `.planning/phases/03-new-phase/03-01-SUMMARY.md` 생성
</output>
