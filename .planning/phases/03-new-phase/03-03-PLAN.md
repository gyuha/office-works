---
phase: 03-new-phase
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java
autonomous: true
requirements:
  - API-02
  - SEC-02
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "ADMIN 토큰으로 GET /api/menus 호출 시 200 + 메뉴 목록(menuId, code, name, displayOrder, isActive)이 반환된다"
    - "USER 토큰으로 GET /api/menus 호출 시 403 + $.code='MENU_002' envelope이 반환된다"
    - "Bearer 토큰 없이 GET /api/menus 호출 시 401이 반환된다"
  artifacts:
    - path: "api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java"
      provides: "Phase 3 테스트 케이스 3개 추가"
      contains: "getMenus_withAdminToken"
  key_links:
    - from: "MenuControllerIT.getMenus_withUserToken_returns403"
      to: "ApiAccessDeniedHandler.handle()"
      via: "HTTP 403 + $.code='MENU_002' assertion"
      pattern: "MENU_002"
    - from: "MenuControllerIT.getMenus_withAdminToken_returns200"
      to: "MenuController.getMenus()"
      via: "HTTP 200 + $.data[0].code assertion"
      pattern: "data\\[0\\]"
---

<objective>
MenuControllerIT에 GET /api/menus 엔드포인트의 Phase 3 시나리오 3개를 추가한다.

Purpose: Plan 01/02 산출물(AdminMenuResponse + GET /api/menus + ApiAccessDeniedHandler + SecurityConfig)을 End-to-End로 검증한다. 기존 Phase 2 테스트는 변경하지 않는다.
Output: MenuControllerIT에 3개 @Test 메서드 추가
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/office-works/.planning/PROJECT.md
@/Users/gyuha/workspace/office-works/.planning/ROADMAP.md

<interfaces>
<!-- 기존 코드 — Executor는 이 계약을 직접 사용한다. 탐색 불필요. -->

From MenuControllerIT.java (수정 대상 — 기존 구조 유지, 메서드만 추가):
  - @SpringBootTest, @AutoConfigureWebTestClient, @ActiveProfiles("local"), @Import(TestcontainersConfig.class)
  - private WebTestClient webTestClient (Autowired)
  - private JdbcTemplate jdbcTemplate (Autowired)
  - private String userAccessToken, adminAccessToken (BeforeEach에서 설정)
  - BeforeEach setUp(): UUID uid, registerUser(email), login(email), jdbcTemplate.update(UPDATE_ROLE_SQL, adminEmail)
  - private void registerUser(String email): POST /api/v1/auth/register, 201 기대
  - private String login(String email): POST /api/v1/auth/login, $.data.accessToken 추출

기존 GET /api/menus/my 시나리오 (변경 금지):
  - getMyMenus_withoutToken_returns401()
  - getMyMenus_withUserNoRoles_returnsEmptyArray()
  - getMyMenus_withAdminToken_returnsAllMenusWithFullPermission()

추가할 GET /api/menus 시나리오 (D-15, D-16 per Phase 3 CONTEXT):
  1. getMenus_withAdminToken_returns200WithFields()
     - URI: /api/menus
     - Header: Authorization Bearer adminAccessToken
     - 기대: 200 OK
     - jsonPath 검증: $.code = "SUCCESS", $.data 배열, $.data[0].code 존재, $.data[0].name 존재, $.data[0].menuId 존재, $.data[0].displayOrder 존재, $.data[0].isActive 존재

  2. getMenus_withUserToken_returns403WithApiResponseEnvelope()
     - URI: /api/menus
     - Header: Authorization Bearer userAccessToken
     - 기대: 403 Forbidden
     - jsonPath 검증: $.code = "MENU_002", $.message 존재

  3. getMenus_withoutToken_returns401()
     - URI: /api/menus
     - Header: 없음
     - 기대: 401 Unauthorized

Phase 2 Lesson 적용:
  - IT 테스트에 ADMIN 응답 필드 명시적 검증 필요 ($.data[0].isActive 등 구체적 assertion)
  - userAccessToken은 역할 미할당 USER — 403 테스트에 적합
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MenuControllerIT에 GET /api/menus 테스트 케이스 3개 추가</name>
  <files>api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java</files>
  <read_first>
    - api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java (기존 전체 구조)
    - api/src/test/java/com/example/bootstrap/global/TestcontainersConfig.java (Testcontainers 설정 확인)
  </read_first>
  <behavior>
    - Test 1 (ADMIN 200): GET /api/menus + adminAccessToken → 200, $.code="SUCCESS", $.data는 배열, $.data[0].code 존재, $.data[0].name 존재, $.data[0].menuId 존재, $.data[0].displayOrder 존재, $.data[0].isActive 존재
    - Test 2 (USER 403): GET /api/menus + userAccessToken → 403, $.code="MENU_002", $.message 비어있지 않음
    - Test 3 (미인증 401): GET /api/menus + 토큰 없음 → 401
  </behavior>
  <action>
    기존 `MenuControllerIT.java` 끝에 3개 @Test 메서드를 추가한다. 기존 코드(import, setUp, registerUser, login, 기존 3개 테스트)는 변경하지 않는다.

    추가할 메서드 3개:

    **메서드 1: getMenus_withAdminToken_returns200WithFields**
    - @Test, @DisplayName("GET /api/menus — ADMIN 토큰으로 요청하면 200과 전체 메뉴 목록을 반환한다")
    - webTestClient.get().uri("/api/menus") + Authorization adminAccessToken
    - .expectStatus().isOk()
    - .expectBody()
      .jsonPath("$.code").isEqualTo("SUCCESS")
      .jsonPath("$.data").isArray()
      .jsonPath("$.data[0].menuId").isNotEmpty()
      .jsonPath("$.data[0].code").isNotEmpty()
      .jsonPath("$.data[0].name").isNotEmpty()
      .jsonPath("$.data[0].displayOrder").isNotEmpty()
      .jsonPath("$.data[0].isActive").isNotEmpty()

    **메서드 2: getMenus_withUserToken_returns403WithApiResponseEnvelope**
    - @Test, @DisplayName("GET /api/menus — USER 토큰으로 요청하면 403 Forbidden과 MENU_002 에러 응답을 반환한다")
    - webTestClient.get().uri("/api/menus") + Authorization userAccessToken
    - .expectStatus().isForbidden()
    - .expectBody()
      .jsonPath("$.code").isEqualTo("MENU_002")
      .jsonPath("$.message").isNotEmpty()

    **메서드 3: getMenus_withoutToken_returns401**
    - @Test, @DisplayName("GET /api/menus — Bearer 토큰 없이 요청하면 401을 반환한다")
    - webTestClient.get().uri("/api/menus")
    - .expectStatus().isUnauthorized()

    주의:
    - Phase 2 Lesson: ADMIN 응답의 필드를 구체적으로 assertion ($.data[0].isActive 포함)
    - Phase 2 Lesson: $.data[0]가 존재하기 위해 DB에 활성 메뉴가 있어야 함 — Flyway 초기 INSERT (Phase 1 SCH-06)로 메뉴 데이터가 이미 존재한다고 가정. ADMIN은 findAll() 사용이므로 is_active 여부 무관하게 메뉴 존재.
    - userAccessToken 계정은 BeforeEach에서 역할 미할당으로 생성됨 → USER 403 테스트에 그대로 사용 가능.
    - 중복 시나리오 없음: getMenus_withoutToken_returns401은 /api/menus 대상, 기존 getMyMenus_withoutToken_returns401은 /api/menus/my 대상으로 서로 다름.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew test --tests "com.example.bootstrap.menu.controller.MenuControllerIT" -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - MenuControllerIT에 3개 테스트 메서드 추가됨
    - 모든 6개 테스트(Phase 2 기존 3개 + Phase 3 신규 3개)가 PASSED
    - getMenus_withAdminToken_returns200WithFields: 200 + 필드 검증 통과
    - getMenus_withUserToken_returns403WithApiResponseEnvelope: 403 + $.code="MENU_002" 검증 통과
    - getMenus_withoutToken_returns401: 401 검증 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 → WebTestClient → GET /api/menus | End-to-End 요청 경로 전체 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | IT 테스트 ADMIN 승격 패턴 | accept | jdbcTemplate.update()로 테스트 DB만 조작. Testcontainers 격리 환경 사용 |
| T-03-08 | Information Disclosure | 테스트 코드 내 하드코딩 자격증명 | accept | 테스트 전용 계정(pass1234), 프로덕션 자격증명 없음 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.example.bootstrap.menu.controller.MenuControllerIT"` 성공 (6개 테스트 PASSED)
- 403 응답 $.code="MENU_002" 검증 포함 확인: `grep -c "MENU_002" api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java`
- getMenus_withAdminToken 테스트 존재 확인: `grep -c "getMenus_withAdminToken" api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java`
</verification>

<success_criteria>
1. MenuControllerIT에 3개 메서드 추가 (getMenus_withAdminToken_returns200WithFields, getMenus_withUserToken_returns403WithApiResponseEnvelope, getMenus_withoutToken_returns401)
2. 기존 Phase 2 테스트 3개는 변경되지 않는다
3. ADMIN 200 테스트: $.data[0].menuId, $.data[0].code, $.data[0].name, $.data[0].displayOrder, $.data[0].isActive 모두 assertion
4. USER 403 테스트: $.code="MENU_002" 명시적 assertion
5. 6개 테스트 전체 PASSED (./gradlew test --tests "...MenuControllerIT")
</success_criteria>

<output>
완료 후 `.planning/phases/03-new-phase/03-03-SUMMARY.md` 생성
</output>
