---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java
  - api/src/test/java/com/example/bootstrap/menu/application/service/MenuPermissionServiceTest.java
autonomous: true
requirements:
  - DOM-06
  - SEC-01
must_haves:
  truths:
    - "ADMIN 사용자(isAdmin=true)로 getMyMenus() 호출 시 DB 권한 테이블 조회 없이 전체 활성 메뉴를 canRead=true, canWrite=true로 반환한다"
    - "역할 미할당 사용자(roleIds 빈 리스트)가 getMyMenus() 호출 시 findByRoleIdIn()을 건너뛰고 빈 배열을 반환한다 (500 없음)"
    - "두 역할의 권한이 OR 집계된다 — 역할A(canRead=true, canWrite=false) + 역할B(canRead=false, canWrite=true) → canRead=true, canWrite=true"
    - "user_menu_permissions 행이 있으면 역할 권한을 덮어쓴다 (양방향 오버라이드)"
    - "canRead OR canWrite 둘 다 false인 메뉴는 응답에서 제외된다"
    - "canRead(userId, menuCode) / canWrite(userId, menuCode) 단건 조회 메서드가 Mono<Boolean>을 반환한다"
    - "MenuPermissionServiceTest의 모든 단위 테스트가 통과한다"
  artifacts:
    - path: "api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java"
      provides: "getMyMenus(Long, boolean), canRead(Long, String), canWrite(Long, String)"
      exports: ["getMyMenus", "canRead", "canWrite"]
    - path: "api/src/test/java/com/example/bootstrap/menu/application/service/MenuPermissionServiceTest.java"
      provides: "단위 테스트 — 7개 시나리오"
  key_links:
    - from: "MenuPermissionService.getMyMenus()"
      to: "UserRoleRepository.findByUserId()"
      via: "역할 목록 조회"
    - from: "MenuPermissionService.getMyMenus()"
      to: "Mono.zip(rolePermsMono, userPermsMono)"
      via: "병렬 권한 조회 (roleIds 비어있지 않을 때)"
    - from: "MenuPermissionService.getMyMenus()"
      to: "MenuRepository.findAllById(menuIds)"
      via: "접근 가능한 menuId로 메뉴 정보 조회 (D-12)"
    - from: "MenuPermissionService.canRead()"
      to: "MenuRepository.findByCode()"
      via: "menuCode로 menu 조회 후 권한 확인"
---

<objective>
권한 병합 서비스 MenuPermissionService를 구현하고 단위 테스트로 검증한다.

Purpose: Phase 2 핵심 로직 — ADMIN bypass, 역할 OR 집계, 개인 오버라이드 병합,
         빈 roleIds 안전 처리, MenuAuthorizationBean용 단건 조회 메서드 제공.
Output:
  1. MenuPermissionService.java — getMyMenus() + canRead() + canWrite() (신규 생성)
  2. MenuPermissionServiceTest.java — @ExtendWith(MockitoExtension) 단위 테스트 (신규 생성)
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/office-works/.planning/ROADMAP.md
@/Users/gyuha/workspace/office-works/.planning/phases/02-new-phase/02-CONTEXT.md
@/Users/gyuha/workspace/office-works/.planning/phases/02-new-phase/02-PATTERNS.md

<interfaces>
<!-- Plan 01 산출물 — 이 Plan의 의존성 -->

From menu/application/dto/MyMenuResponse.java (Plan 01 생성):
  public record MyMenuResponse(Long menuId, String code, String name,
                               boolean canRead, boolean canWrite)
  accessor: menuId(), code(), name(), canRead(), canWrite()  ← () 반환

From menu/domain/repository/MenuRepository.java (Plan 01 수정):
  findByCode(String code): Mono<Menu>
  findByIsActiveTrueOrderByDisplayOrderAsc(): Flux<Menu>
  findAllById(Iterable<Long> ids): Flux<Menu>  ← ReactiveCrudRepository 기본

From menu/domain/repository/UserRoleRepository.java:
  findByUserId(Long userId): Flux<UserRole>

From menu/domain/repository/RoleMenuPermissionRepository.java:
  findByRoleIdIn(Collection<Long> roleIds): Flux<RoleMenuPermission>

From menu/domain/repository/UserMenuPermissionRepository.java:
  findByUserId(Long userId): Flux<UserMenuPermission>

From menu/domain/model/Menu.java:
  getId(): Long
  getCode(): String
  getName(): String
  getDisplayOrder(): Integer
  isActive(): Boolean       ← Menu 엔티티 한정 isActive() — Boolean 박싱

From menu/domain/model/UserRole.java:
  getRoleId(): Long

From menu/domain/model/RoleMenuPermission.java:
  getMenuId(): Long
  getCanRead(): Boolean     ← getCanRead() (is 접두사 아님)
  getCanWrite(): Boolean    ← getCanWrite() (is 접두사 아님)

From menu/domain/model/UserMenuPermission.java:
  getMenuId(): Long
  getCanRead(): Boolean
  getCanWrite(): Boolean

From global/exception/ErrorCode.java (Plan 01 추가):
  MENU_001  ← menuCode에 해당하는 메뉴 없을 때 (canRead/canWrite 단건 조회)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MenuPermissionServiceTest 작성 (단위 테스트)</name>
  <files>api/src/test/java/com/example/bootstrap/menu/application/service/MenuPermissionServiceTest.java</files>
  <read_first>
    - api/src/test/java/com/example/bootstrap/account/application/service/AuthServiceTest.java (클래스 구조, StepVerifier 패턴, @BeforeEach 수동 생성 패턴)
    - 02-PATTERNS.md MenuPermissionServiceTest 섹션 (StepVerifier, 헬퍼 빌더 패턴)
    - 02-CONTEXT.md D-01~D-12 (병합 규칙 전체)
  </read_first>
  <behavior>
    테스트 시나리오 (메서드명: methodUnderTest_scenario_expectation):

    1. getMyMenus_withAdminRole_returnsAllActiveMenus
       - menuRepository.findByIsActiveTrueOrderByDisplayOrderAsc()가 Menu 2개 반환
       - getMyMenus(userId, true) 호출 → List 크기 2, 첫 번째 canRead=true, canWrite=true
       - userRoleRepository 호출 없음 (verify never)

    2. getMyMenus_withEmptyRoles_returnsEmptyList
       - userRoleRepository.findByUserId(userId) → Flux.empty()
       - userMenuPermissionRepository.findByUserId(userId) → Flux.empty()
       - getMyMenus(userId, false) 호출 → 빈 List 반환 (500 아님)
       - roleMenuPermissionRepository.findByRoleIdIn() 호출 없음 (verify never)

    3. getMyMenus_withRolePermissions_returnsAccessibleMenus
       - userRoleRepository → roleId=1L 반환
       - roleMenuPermissionRepository.findByRoleIdIn([1L]) → menuId=10L, canRead=true, canWrite=false 반환
       - userMenuPermissionRepository → Flux.empty()
       - menuRepository.findAllById([10L]) → Menu(id=10, isActive=true, displayOrder=1) 반환
       - getMyMenus(userId, false) → List 크기 1, canRead=true, canWrite=false

    4. getMyMenus_withUserOverride_overridePrevailsOverRolePermission
       - 역할 권한: menuId=10L, canRead=true, canWrite=false
       - 개인 오버라이드: menuId=10L, canRead=false, canWrite=true (D-01: 양방향 오버라이드)
       - getMyMenus(userId, false) → canRead=false, canWrite=true (오버라이드 우선)

    5. getMyMenus_withMultipleRoles_ORAggregatesPermissions
       - 역할A: menuId=10L, canRead=true, canWrite=false
       - 역할B: menuId=10L, canRead=false, canWrite=true
       - getMyMenus(userId, false) → canRead=true, canWrite=true (OR 집계, D-04)

    6. getMyMenus_withNoPermission_excludesMenuFromResult
       - 역할 권한: menuId=10L, canRead=false, canWrite=false
       - 개인 오버라이드: 없음
       - getMyMenus(userId, false) → 빈 List (D-03: 둘 다 false인 메뉴 제외)

    7. canRead_whenMenuCodeNotFound_returnsFalse
       - menuRepository.findByCode("UNKNOWN") → Mono.empty()
       - canRead(userId, "UNKNOWN") → Mono<false>

    헬퍼 메서드 (static private):
      buildMenu(Long id, String code, String name, int displayOrder): Menu (isActive=true 고정)
      buildRolePerm(Long menuId, boolean canRead, boolean canWrite): RoleMenuPermission
      buildUserPerm(Long menuId, boolean canRead, boolean canWrite): UserMenuPermission
  </behavior>
  <action>
    api/src/test/java/com/example/bootstrap/menu/application/service/ 디렉토리 생성 후
    MenuPermissionServiceTest.java를 작성한다.

    패키지: com.example.bootstrap.menu.application.service

    어노테이션:
      @ExtendWith(MockitoExtension.class)  ← Spring context 없음, @SpringBootTest 금지

    @Mock 필드:
      UserRoleRepository userRoleRepository
      RoleMenuPermissionRepository roleMenuPermissionRepository
      UserMenuPermissionRepository userMenuPermissionRepository
      MenuRepository menuRepository

    @BeforeEach: menuPermissionService = new MenuPermissionService(4개 리포지토리) 수동 생성

    각 테스트: StepVerifier.create(...).assertNext(list -> { assertThat(list)... }).verifyComplete()
    에러 케이스: .defaultIfEmpty(false) 동작 확인 — StepVerifier.create().expectNext(false).verifyComplete()

    Mock 설정: when(repo.method(...)).thenReturn(Flux.just(...)) 또는 Mono.just(...)

    이 시점에서 MenuPermissionService.java가 아직 없으므로 컴파일은 실패한다.
    테스트 파일만 작성하고 Task 2에서 서비스를 구현한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileTestJava -q 2>&1 | grep -i "error\|warning" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MenuPermissionServiceTest.java 파일이 존재한다
    - @ExtendWith(MockitoExtension.class) 어노테이션이 클래스에 선언된다
    - 7개 이상의 @Test 메서드가 있다
    - StepVerifier.create() 호출이 각 테스트에 존재한다
    - Spring context 로딩 어노테이션(@SpringBootTest, @Import 등)이 없다
    - Task 2 완료 후 ./gradlew test --tests "*.MenuPermissionServiceTest" 가 통과한다
  </acceptance_criteria>
  <done>테스트 파일 작성 완료. Task 2에서 서비스 구현 후 통과 확인.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MenuPermissionService 구현</name>
  <files>api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java</files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/account/application/service/AuthService.java (Reactive flatMap 체인, 생성자 주입 패턴)
    - 02-PATTERNS.md MenuPermissionService 섹션 전체 (병합 알고리즘, ADMIN bypass)
    - 02-RESEARCH.md Code Examples 섹션 전체 (USER 경로 전체 플로우, canRead/canWrite 단건 조회)
    - 02-CONTEXT.md D-01~D-16 (모든 결정)
  </read_first>
  <behavior>
    구현해야 할 동작:

    getMyMenus(Long userId, boolean isAdmin): Mono<List<MyMenuResponse>>
    - isAdmin=true 분기: menuRepository.findByIsActiveTrueOrderByDisplayOrderAsc() 호출,
      전체 메뉴를 canRead=true, canWrite=true로 매핑, collectList() 반환
    - isAdmin=false 분기:
      1. userRoleRepository.findByUserId(userId).map(UserRole::getRoleId).collectList()
      2. roleIds가 비어 있으면 rolePermsMono = Mono.just(Collections.emptyList())
         roleIds가 있으면 roleMenuPermissionRepository.findByRoleIdIn(roleIds).collectList()
      3. userPermsMono = userMenuPermissionRepository.findByUserId(userId).collectList() (항상 실행)
      4. Mono.zip(rolePermsMono, userPermsMono)로 병렬 실행
      5. tuple.getT1()으로 OR 집계: Map<Long menuId, boolean[]{canRead, canWrite}>
         - map.merge(menuId, new boolean[]{cr, cw}, (e, i) -> new boolean[]{e[0]||i[0], e[1]||i[1]})
      6. tuple.getT2()로 오버라이드 적용: permMap.put(menuId, new boolean[]{cr, cw}) 덮어씀
      7. 접근 가능 menuId: e.getValue()[0] || e.getValue()[1] 인 항목만 추출
      8. 빈 경우 Mono.just(emptyList) 즉시 반환
      9. menuRepository.findAllById(accessibleMenuIds)
         .filter(menu -> Boolean.TRUE.equals(menu.isActive()))
         .sort(Comparator.comparingInt(Menu::getDisplayOrder))
         .map(menu -> new MyMenuResponse(menu.getId(), menu.getCode(), menu.getName(),
                        permMap.get(menu.getId())[0], permMap.get(menu.getId())[1]))
         .collectList()

    canRead(Long userId, String menuCode): Mono<Boolean>
    - menuRepository.findByCode(menuCode) → Mono.empty()이면 defaultIfEmpty(false)
    - 메뉴 존재 시: getMyMenus(userId, false)를 호출하여 해당 menuId 항목 찾아 canRead() 반환
    - .defaultIfEmpty(false) 마지막에 추가

    canWrite(Long userId, String menuCode): Mono<Boolean>
    - canRead()와 동일 구조, canWrite() 반환

    클래스 어노테이션: @Service
    생성자 주입: final 파라미터 4개 (UserRoleRepository, RoleMenuPermissionRepository,
                                 UserMenuPermissionRepository, MenuRepository)
    Lombok 금지, @Autowired 금지

    Boolean null-safe 비교: Boolean.TRUE.equals(p.getCanRead()) — getCanRead()가 null이면 false 처리
  </behavior>
  <action>
    api/src/main/java/com/example/bootstrap/menu/application/service/ 디렉토리 생성 후
    MenuPermissionService.java를 구현한다.

    패키지: com.example.bootstrap.menu.application.service

    필요한 import 목록:
      com.example.bootstrap.global.exception.BusinessException
      com.example.bootstrap.global.exception.ErrorCode
      com.example.bootstrap.menu.application.dto.MyMenuResponse
      com.example.bootstrap.menu.domain.model.Menu
      com.example.bootstrap.menu.domain.model.RoleMenuPermission
      com.example.bootstrap.menu.domain.model.UserMenuPermission
      com.example.bootstrap.menu.domain.model.UserRole
      com.example.bootstrap.menu.domain.repository.MenuRepository
      com.example.bootstrap.menu.domain.repository.RoleMenuPermissionRepository
      com.example.bootstrap.menu.domain.repository.UserMenuPermissionRepository
      com.example.bootstrap.menu.domain.repository.UserRoleRepository
      org.springframework.stereotype.Service
      reactor.core.publisher.Mono
      java.util.Collections
      java.util.Comparator
      java.util.HashMap
      java.util.List
      java.util.Map

    private 메서드:
      buildRolePermissionMap(List<RoleMenuPermission>): Map<Long, boolean[]>
      applyUserOverrides(Map<Long, boolean[]>, List<UserMenuPermission>): Map<Long, boolean[]>
      (Map을 mutate하여 반환 — 병합 과정에서 새 Map이 아닌 기존 Map을 변경)

    canRead/canWrite 단건 조회: 02-RESEARCH.md "canRead/canWrite 단건 조회" 코드 예제를 참고하되,
    BusinessException 대신 defaultIfEmpty(false) 사용 (menuCode 없을 때 false 반환이 올바른 동작).
    findByCode()가 empty이면 false 반환, 존재하면 getMyMenus() 결과에서 canRead/canWrite 추출.

    구현 후 MenuPermissionServiceTest의 모든 테스트가 통과해야 한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew test --tests "com.example.bootstrap.menu.application.service.MenuPermissionServiceTest" -x integrationTest 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MenuPermissionService.java 파일이 존재한다
    - @Service 어노테이션이 클래스에 선언된다
    - public Mono&lt;List&lt;MyMenuResponse&gt;&gt; getMyMenus(Long userId, boolean isAdmin) 메서드가 존재한다
    - public Mono&lt;Boolean&gt; canRead(Long userId, String menuCode) 메서드가 존재한다
    - public Mono&lt;Boolean&gt; canWrite(Long userId, String menuCode) 메서드가 존재한다
    - 생성자 파라미터 4개 (UserRoleRepository, RoleMenuPermissionRepository, UserMenuPermissionRepository, MenuRepository)가 존재한다
    - @Autowired, @RequiredArgsConstructor, @AllArgsConstructor 어노테이션이 없다
    - ./gradlew test --tests "*.MenuPermissionServiceTest" 가 BUILD SUCCESSFUL로 종료된다
    - Mono.zip() 사용 코드에서 roleIds 빈 체크 분기가 존재한다
      (grep 확인: grep -n "isEmpty\|emptyList\|Mono.just" MenuPermissionService.java)
  </acceptance_criteria>
  <done>MenuPermissionService 구현 완료, 7개 단위 테스트 전체 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Service | userId는 SecurityContext에서만 추출 (요청 파라미터 신뢰 불가) |
| Service → Repository | roleIds 빈 리스트 시 findByRoleIdIn 건너뜀 — R2DBC IN () 동작 불확실성 제거 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Elevation of Privilege | MenuPermissionService.getMyMenus() — userId 파라미터 | mitigate | userId는 컨트롤러가 authentication.getPrincipal()에서만 추출. 서비스 자체는 파라미터를 신뢰하지 않음 (컨트롤러가 신뢰 경계) |
| T-02-03 | Information Disclosure | getMyMenus() — canRead/canWrite 모두 false인 메뉴 포함 가능성 | mitigate | D-03 구현: permMap에서 e.getValue()[0] \|\| e.getValue()[1] 조건 — 둘 다 false이면 accessibleMenuIds에 포함 안 됨 |
| T-02-04 | Spoofing | ADMIN bypass — isAdmin 파라미터 조작 | mitigate | isAdmin은 컨트롤러에서 authentication.getAuthorities()로만 판정. 서비스 파라미터는 컨트롤러 책임 (D-17) |
| T-02-05 | Denial of Service | Mono.zip() + 빈 리스트 → empty 반환 → 무한 대기 가능성 | mitigate | D-09: roleIds 빈 체크 후 Mono.just(emptyList) 사용 — Mono.empty() 전달 금지 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 이 Plan에 패키지 설치 없음 |
</threat_model>

<verification>
1. 단위 테스트 전체 통과:
   cd /Users/gyuha/workspace/office-works/api && ./gradlew test --tests "com.example.bootstrap.menu.application.service.MenuPermissionServiceTest" -x integrationTest

2. roleIds 빈 분기 존재 확인:
   grep -n "isEmpty\|Mono.just(Collections" api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java

3. Boolean null-safe 비교 확인 (isCanRead 패턴 없음):
   grep -v "getCanRead\|getCanWrite" api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java | grep -c "canRead\|canWrite"

4. ADMIN bypass 분기 확인:
   grep -n "isAdmin\|ROLE_ADMIN\|findByIsActiveTrue" api/src/main/java/com/example/bootstrap/menu/application/service/MenuPermissionService.java
</verification>

<success_criteria>
- MenuPermissionService 3개 public 메서드 (getMyMenus, canRead, canWrite) 구현 완료
- MenuPermissionServiceTest 7개 테스트 전체 BUILD SUCCESSFUL
- roleIds 빈 체크 명시적 분기 존재
- isCanRead/isCanWrite 사용 없음 (Boolean getter 패턴 준수)
</success_criteria>

<output>
완료 후 .planning/phases/02-new-phase/02-02-SUMMARY.md 생성
</output>
