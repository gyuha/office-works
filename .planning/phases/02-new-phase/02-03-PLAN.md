---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java
  - api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java
  - api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java
autonomous: true
requirements:
  - DOM-07
  - API-01
  - SEC-01
must_haves:
  truths:
    - "GET /api/menus/my 호출 시 menuId, code, name, canRead, canWrite 필드를 포함한 JSON 배열을 반환한다"
    - "미인증 요청(Bearer 없음)은 401 Unauthorized를 반환한다"
    - "ADMIN 토큰으로 GET /api/menus/my 호출 시 전체 활성 메뉴를 canRead=true, canWrite=true로 받는다"
    - "역할 미할당 USER 토큰으로 호출 시 빈 배열([])과 200을 반환한다"
    - "MenuAuthorizationBean이 Spring 컨텍스트에 menuAuthorizationBean 이름으로 등록된다"
    - "MenuAuthorizationBean.canRead(Authentication, menuCode)와 canWrite(Authentication, menuCode)가 Mono<Boolean>을 반환한다"
  artifacts:
    - path: "api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java"
      provides: "GET /api/menus/my 엔드포인트"
      exports: ["getMyMenus"]
    - path: "api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java"
      provides: "canRead(Authentication, String), canWrite(Authentication, String)"
    - path: "api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java"
      provides: "통합 테스트 — 4개 시나리오"
  key_links:
    - from: "MenuController.getMyMenus()"
      to: "authentication.getPrincipal()"
      via: "userId = (Long) authentication.getPrincipal()"
    - from: "MenuController.getMyMenus()"
      to: "authentication.getAuthorities()"
      via: "ROLE_ADMIN 확인 → isAdmin 플래그"
    - from: "MenuController.getMyMenus()"
      to: "MenuPermissionService.getMyMenus()"
      via: "userId, isAdmin 전달"
    - from: "MenuAuthorizationBean.canRead()"
      to: "MenuPermissionService.canRead(userId, menuCode)"
      via: "userId = (Long) authentication.getPrincipal()"
    - from: "GET /api/menus/my"
      to: "SecurityConfig.anyExchange().authenticated()"
      via: "미인증 시 자동 401 — SecurityConfig 수정 불필요"
---

<objective>
MenuController(GET /api/menus/my 엔드포인트)와 MenuAuthorizationBean을 구현하고
통합 테스트로 엔드 투 엔드 동작을 검증한다.

Purpose: Phase 2 Core Value 달성 — "인증된 사용자가 자신의 접근 가능한 메뉴 목록을 API로 받는다"
Output:
  1. MenuController.java — GET /api/menus/my (신규 생성)
  2. MenuAuthorizationBean.java — canRead/canWrite(Authentication, menuCode) (신규 생성)
  3. MenuControllerIT.java — @SpringBootTest 통합 테스트 (신규 생성)
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/office-works/.planning/ROADMAP.md
@/Users/gyuha/workspace/office-works/.planning/phases/02-new-phase/02-CONTEXT.md
@/Users/gyuha/workspace/office-works/.planning/phases/02-new-phase/02-PATTERNS.md
@/Users/gyuha/workspace/office-works/.planning/phases/02-new-phase/02-02-SUMMARY.md

<interfaces>
<!-- Plan 02 산출물 — 이 Plan의 의존성 -->

From menu/application/service/MenuPermissionService.java (Plan 02 생성):
  @Service
  public class MenuPermissionService {
    public Mono<List<MyMenuResponse>> getMyMenus(Long userId, boolean isAdmin)
    public Mono<Boolean> canRead(Long userId, String menuCode)
    public Mono<Boolean> canWrite(Long userId, String menuCode)
  }

From menu/application/dto/MyMenuResponse.java (Plan 01 생성):
  public record MyMenuResponse(Long menuId, String code, String name,
                               boolean canRead, boolean canWrite)

From global/response/ApiResponse.java:
  static success(String message, T data): ApiResponse<T>
  응답 JSON 형태: { "code": "SUCCESS", "message": "...", "data": [...] }

From global/security/jwt/JwtAuthenticationFilter.java (기존):
  UsernamePasswordAuthenticationToken 생성 시:
    principal = Long userId (parseLong(claims.getSubject()))
    authorities = [new SimpleGrantedAuthority("ROLE_" + role)]
  → authentication.getPrincipal() 반환 타입: Long (Long.parseLong 결과, Long 박싱)
  → authentication.getAuthorities()에서 "ROLE_ADMIN" 확인

From global/config/SecurityConfig.java (기존, 수정 없음):
  anyExchange().authenticated() 규칙이 이미 /api/menus/my에 적용됨
  → 미인증 요청 자동 401

ADMIN 계정 생성 방법 (통합 테스트용):
  계정 생성 후 DB에서 users.role = 'ADMIN'으로 직접 UPDATE하거나,
  AuthControllerIT.java 패턴 참조 — /api/v1/auth/register + /api/v1/auth/login 순서
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MenuController + MenuAuthorizationBean 구현</name>
  <files>
    api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java,
    api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java
  </files>
  <read_first>
    - api/src/main/java/com/example/bootstrap/account/controller/AccountController.java (Authentication 주입, ApiResponse 래핑, 생성자 주입 패턴)
    - api/src/main/java/com/example/bootstrap/global/security/jwt/JwtAuthenticationFilter.java (lines 60~70: principal 세팅 방식, ROLE_ 접두사 확인)
    - api/src/main/java/com/example/bootstrap/global/config/SecurityConfig.java (anyExchange().authenticated() 확인)
    - 02-PATTERNS.md MenuController 섹션, MenuAuthorizationBean 섹션
    - 02-CONTEXT.md D-13~D-16 (MenuAuthorizationBean 결정)
  </read_first>
  <action>
    [MenuController.java 신규 생성]
    위치: api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java
    패키지: com.example.bootstrap.menu.controller

    어노테이션: @RestController, @RequestMapping("/api/menus")
    의존성 필드: private final MenuPermissionService menuPermissionService (생성자 주입)
    Lombok 금지, @Autowired 금지.

    메서드:
      @GetMapping("/my")
      public Mono<ResponseEntity<ApiResponse<List<MyMenuResponse>>>> getMyMenus(
              final Authentication authentication)

      구현:
        Long userId = (Long) authentication.getPrincipal();
        boolean isAdmin = authentication.getAuthorities().stream()
            .anyMatch(a -> a.getAuthority().equals("ROLE_ADMIN"));
        return menuPermissionService.getMyMenus(userId, isAdmin)
            .map(menus -> ResponseEntity.ok(
                ApiResponse.success("접근 가능한 메뉴 목록을 조회했습니다.", menus)));

    필요한 import:
      com.example.bootstrap.menu.application.dto.MyMenuResponse
      com.example.bootstrap.menu.application.service.MenuPermissionService
      com.example.bootstrap.global.response.ApiResponse
      org.springframework.http.ResponseEntity
      org.springframework.security.core.Authentication
      org.springframework.web.bind.annotation.GetMapping
      org.springframework.web.bind.annotation.RequestMapping
      org.springframework.web.bind.annotation.RestController
      reactor.core.publisher.Mono
      java.util.List

    [MenuAuthorizationBean.java 신규 생성]
    위치: api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java
    패키지: com.example.bootstrap.global.security
    (JwtAuthenticationFilter.java가 있는 jwt/ 서브패키지가 아닌, global/security/ 바로 아래)

    어노테이션: @Component("menuAuthorizationBean") — SpEL 참조명 고정
    의존성 필드: private final MenuPermissionService menuPermissionService (생성자 주입)
    Lombok 금지.

    메서드 2개:
      public Mono<Boolean> canRead(final Authentication authentication, final String menuCode)
        Long userId = (Long) authentication.getPrincipal();
        return menuPermissionService.canRead(userId, menuCode);

      public Mono<Boolean> canWrite(final Authentication authentication, final String menuCode)
        Long userId = (Long) authentication.getPrincipal();
        return menuPermissionService.canWrite(userId, menuCode);

    Phase 2에서는 @PreAuthorize를 어디에도 적용하지 않는다 (D-13). Bean 구현만.

    필요한 import:
      com.example.bootstrap.menu.application.service.MenuPermissionService
      org.springframework.security.core.Authentication
      org.springframework.stereotype.Component
      reactor.core.publisher.Mono
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MenuController.java 파일이 존재한다
    - MenuController에 @RestController, @RequestMapping("/api/menus") 어노테이션이 있다
    - getMyMenus(Authentication) 메서드에 @GetMapping("/my")가 선언된다
    - MenuAuthorizationBean.java 파일이 존재한다
    - MenuAuthorizationBean에 @Component("menuAuthorizationBean") 어노테이션이 있다
    - MenuAuthorizationBean에 canRead(Authentication, String): Mono&lt;Boolean&gt; 메서드가 있다
    - MenuAuthorizationBean에 canWrite(Authentication, String): Mono&lt;Boolean&gt; 메서드가 있다
    - MenuAuthorizationBean 패키지가 com.example.bootstrap.global.security이다 (jwt 서브패키지 아님)
    - 어느 파일에도 @PreAuthorize 어노테이션이 없다
    - ./gradlew compileJava -q 가 오류 없이 성공한다
  </acceptance_criteria>
  <done>MenuController, MenuAuthorizationBean 컴파일 성공, @PreAuthorize 미적용 확인</done>
</task>

<task type="auto">
  <name>Task 2: MenuControllerIT 통합 테스트</name>
  <files>api/src/test/java/com/example/bootstrap/menu/controller/MenuControllerIT.java</files>
  <read_first>
    - api/src/test/java/com/example/bootstrap/account/controller/AccountControllerIT.java (클래스 어노테이션, @BeforeEach 토큰 획득, WebTestClient jsonPath 패턴)
    - api/src/test/java/com/example/bootstrap/global/TestcontainersConfig.java (TestcontainersConfig import 경로 확인)
    - 02-PATTERNS.md MenuControllerIT 섹션 전체 (ADMIN 계정 준비 방법)
  </read_first>
  <action>
    api/src/test/java/com/example/bootstrap/menu/controller/ 디렉토리 생성 후
    MenuControllerIT.java를 작성한다.

    패키지: com.example.bootstrap.menu.controller

    클래스 어노테이션:
      @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
      @AutoConfigureWebTestClient
      @ActiveProfiles("local")
      @Import(TestcontainersConfig.class)
      @DisplayName("MenuController 통합 테스트")

    필드:
      @Autowired WebTestClient webTestClient
      private String userAccessToken     // USER 역할 토큰
      private String adminAccessToken    // ADMIN 역할 토큰

    @BeforeEach setUp():
      1. USER 계정 등록: POST /api/v1/auth/register (랜덤 이메일, 비밀번호)
      2. USER 로그인: POST /api/v1/auth/login → userAccessToken 추출
      3. ADMIN 계정 등록: POST /api/v1/auth/register (별도 이메일)
      4. ADMIN 로그인 → adminAccessToken 임시 추출
         → 이 토큰은 ROLE_USER 발급 상태. ADMIN 시나리오 테스트를 위해
           DB에서 해당 계정의 users.role = 'ADMIN'으로 UPDATE 후 재로그인.
         아니면: adminUser 등록 후 R2DBC로 직접 users.role 업데이트하는 방식은
                 통합 테스트 복잡도가 높으므로, ADMIN 시나리오는 별도 테스트로 분리하고
                 @Sql 또는 @BeforeEach에서 JDBC로 UPDATE 실행.
         실용적 대안: AccountControllerIT.java 패턴을 확인하여 기존 프로젝트에서
                      ADMIN 계정을 준비하는 방식이 있는지 먼저 파악하고 그 방식을 따른다.
                      없으면 USER 역할만으로 테스트하고, ADMIN 시나리오는
                      @Sql("insert into user_roles...") 방식 또는 TestcontainersConfig에
                      추가 설정으로 처리한다.

    테스트 메서드 4개:

    1. getMyMenus_withValidUserToken_returnsOkWithArray
       - webTestClient.get().uri("/api/menus/my")
         .header("Authorization", "Bearer " + userAccessToken)
         .exchange()
         .expectStatus().isOk()
         .expectBody()
         .jsonPath("$.code").isEqualTo("SUCCESS")
         .jsonPath("$.data").isArray()

    2. getMyMenus_withoutToken_returns401
       - webTestClient.get().uri("/api/menus/my")
         .exchange()
         .expectStatus().isUnauthorized()

    3. getMyMenus_withUserNoRoles_returnsEmptyArray
       - userAccessToken 사용 (역할 없는 계정)
       - .expectBody()
         .jsonPath("$.data").isArray()
         .jsonPath("$.data.length()").isEqualTo(0)  ← 빈 배열 확인

    4. getMyMenus_withAdminToken_returnsAllMenusWithFullPermission (ADMIN 계정 구성 가능 시)
       - adminAccessToken 사용
       - .expectBody()
         .jsonPath("$.data").isArray()
         .jsonPath("$.data[0].canRead").isEqualTo(true)
         .jsonPath("$.data[0].canWrite").isEqualTo(true)

    ADMIN 토큰 준비가 복잡하면 getMyMenus_withAdminToken 테스트에 @Disabled("ADMIN 계정 준비 필요") 추가 후
    나머지 3개 테스트만 통과하도록 구현한다. @Disabled 이유를 주석으로 명확히 기재.

    AccountControllerIT.java의 @BeforeEach 패턴을 그대로 따른다 (webTestClient.post(), .exchange(),
    .expectBody(), .jsonPath().value(token -> this.accessToken = (String) token) 순서).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/office-works/api && ./gradlew test --tests "com.example.bootstrap.menu.controller.MenuControllerIT" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - MenuControllerIT.java 파일이 존재한다
    - @SpringBootTest, @AutoConfigureWebTestClient, @Import(TestcontainersConfig.class) 어노테이션이 있다
    - getMyMenus_withValidUserToken_returnsOkWithArray 테스트가 통과한다
    - getMyMenus_withoutToken_returns401 테스트가 통과한다 (401 검증)
    - getMyMenus_withUserNoRoles_returnsEmptyArray 테스트가 통과한다 (빈 배열 검증)
    - ./gradlew test --tests "*.MenuControllerIT" 가 BUILD SUCCESSFUL로 종료된다 (@Disabled 제외)
    - 응답 JSON $.data가 배열 형태이다
    - 응답 JSON $.code가 "SUCCESS"이다
  </acceptance_criteria>
  <done>MenuControllerIT 통합 테스트 BUILD SUCCESSFUL (미인증 401, USER 빈배열 200, USER 유효 토큰 200 확인)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 요청 → JwtAuthenticationFilter | Bearer 토큰 서명 검증 (기존 필터, 변경 없음) |
| JwtAuthenticationFilter → MenuController | Authentication 객체 신뢰 — principal=Long, authorities=[ROLE_xxx] |
| MenuController → MenuPermissionService | userId는 SecurityContext에서만 추출. 요청 파라미터로 userId 받지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | MenuController — userId 소스 | mitigate | authentication.getPrincipal()만 사용. 요청 헤더/쿼리 파라미터에서 userId 받는 코드 없음 (D-17) |
| T-02-07 | Elevation of Privilege | MenuController — ADMIN 판정 | mitigate | authentication.getAuthorities().stream().anyMatch("ROLE_ADMIN") — DB users.role 재조회 없음 (D-17) |
| T-02-08 | Information Disclosure | GET /api/menus/my — 미인증 응답 | accept | SecurityConfig.anyExchange().authenticated() + HttpStatusServerEntryPoint가 이미 401 반환. 별도 처리 불필요 |
| T-02-09 | Elevation of Privilege | MenuAuthorizationBean — Phase 3 @PreAuthorize 활성화 전 Bean 노출 | accept | Phase 2에서는 @PreAuthorize 미적용. Bean은 등록만 됨. 직접 호출 경로 없음 (D-13) |
| T-02-10 | Information Disclosure | $.data 필드에 메뉴 메타데이터(code, name) 포함 | accept | 인증된 사용자만 접근 가능. 권한 없는 메뉴는 응답에서 제외됨 (D-03). 메뉴 코드/이름 자체는 기밀 아님 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 이 Plan에 패키지 설치 없음 |
</threat_model>

<verification>
1. 통합 테스트 통과:
   cd /Users/gyuha/workspace/office-works/api && ./gradlew test --tests "com.example.bootstrap.menu.controller.MenuControllerIT"

2. 전체 테스트 스위트 통과 (JaCoCo 60% 포함):
   cd /Users/gyuha/workspace/office-works/api && task check

3. MenuAuthorizationBean 패키지 위치 확인:
   grep -n "^package" api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java
   예상: com.example.bootstrap.global.security

4. @PreAuthorize 미사용 확인:
   grep -rn "@PreAuthorize" api/src/main/java/com/example/bootstrap/menu/
   grep -rn "@PreAuthorize" api/src/main/java/com/example/bootstrap/global/security/MenuAuthorizationBean.java
   → 0건이어야 함

5. API 엔드포인트 경로 확인:
   grep -n 'RequestMapping\|GetMapping' api/src/main/java/com/example/bootstrap/menu/controller/MenuController.java
   → "/api/menus" + "/my" 조합 확인
</verification>

<success_criteria>
- GET /api/menus/my: 인증 USER 토큰 → 200 + { code: "SUCCESS", data: [] } (역할 없는 경우)
- GET /api/menus/my: 미인증 → 401
- MenuAuthorizationBean: menuAuthorizationBean 이름으로 컨텍스트 등록
- task check (전체 검증): BUILD SUCCESSFUL (JaCoCo 60%, Checkstyle, SpotBugs 포함)
- @PreAuthorize 어노테이션 0건
</success_criteria>

<output>
완료 후 .planning/phases/02-new-phase/02-03-SUMMARY.md 생성
</output>
